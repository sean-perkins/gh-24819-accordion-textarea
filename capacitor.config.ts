import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-24819-accordion-textarea',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
